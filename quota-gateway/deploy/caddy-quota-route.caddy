# Put these matchers before the generic Sub2API API matcher.  The quota gateway
# proxies the request to the unmodified upstream at 127.0.0.1:18080.
@quota_api path /v1/* /responses* /models* /api/openai/* /api/anthropic/*
handle @quota_api {
  reverse_proxy 127.0.0.1:18193
}
