services:
  quota-gateway:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:18193:18193"
    networks:
      - sub2api-net

networks:
  sub2api-net:
    external: true
    name: sub2api_sub2api-network
